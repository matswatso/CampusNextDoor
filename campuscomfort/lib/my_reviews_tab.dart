import 'package:flutter/material.dart';

class MyReviewsTab extends StatelessWidget {
  const MyReviewsTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My Reviews'));
  }
}